<h1>派送员列表</h1>

<table>
  <thead>
  <tr>
    <th>派送员姓名</th>
    <th>登录名</th>
    <th>密码</th>
    <th>操作</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @sends.each do |send| %>
      <tr>
        <td><%= send.name %></td>
        <td><%= send.login %></td>
        <td>********</td>
        <td><%= link_to 'Edit', edit_send_path(send) %></td>
        <td><%= link_to 'Destroy', send, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<br>

<%= link_to '添加产品', new_send_path %>
